[build]
builder = "nixpacks"
buildCommand = "mvn clean package -DskipTests"

[deploy]
startCommand = "java -jar target/fnet-automation-1.0.0.jar"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
